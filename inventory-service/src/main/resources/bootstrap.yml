server:
  port: 8484
management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    web:
      server:
        request:
          autotime:
            enabled: false
spring:
  application:
    name: inventory-service
  cloud:
    vault:
      scheme: http
      host: localhost
      port: 8200
      token: s.HoFrCRtyeEQdvUSg1UlSqPUI
      authentication: token
eureka:
  instance:
    hostname: inventory-service
    prefer-ip-address: true
    instance-id: ${eureka.instance.hostname}:${server.port}
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/