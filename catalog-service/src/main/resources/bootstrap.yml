server:
  port: 8181
management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    web:
      server:
        request:
          autotime:
            enabled: false
spring:
  application:
    name: catalog-service
  cloud:
    vault:
      scheme: http
      host: localhost
      port: 8200
      token: s.oAn5PoZSJN3o5yrXkHwUErQ2
      authentication: token
    discovery:
      enabled: true
    loadbalancer:
      enabled: true
    config:
      uri: http://localhost:8888
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
    instance-id: ${eureka.instance.hostname}:${server.port}
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true